{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Ubion 에이전트 main window — core APIs, shell sidecar for the Python supervisor, and the updater plugin.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "updater:default"
  ]
}

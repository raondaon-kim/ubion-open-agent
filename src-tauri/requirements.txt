# Runtime dependencies for the embedded Python interpreter shipped
# with the Tauri shell. Versions pinned to what works in dev as of
# 2026-05-14. Updated by re-running scripts/setup-embedded-python.ps1.

# LLM clients
anthropic>=0.40.0
openai>=1.50.0

# HTTP layer (anthropic+openai both pull this transitively, pin to be safe)
httpx>=0.27.0

# FastAPI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# Config
python-dotenv>=1.0.0
PyYAML>=6.0.0
